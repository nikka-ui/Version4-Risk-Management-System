# Placeholder removed — Laravel 11 application lives in /backend.
# The api Dockerfile copies backend/ into the image (public/ is the web root).
# See docs/DOCKER.md and docs/adr/001-backend-laravel.md.
